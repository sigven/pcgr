# Function that produces the contents of sheets for an Excel report of PCGR output

Function that produces the contents of sheets for an Excel report of
PCGR output

## Usage

``` r
get_excel_sheets(report = NULL)
```

## Arguments

- report:

  PCGR report object
