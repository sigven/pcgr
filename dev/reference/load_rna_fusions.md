# Load RNA fusion results

Load RNA fusion results

## Usage

``` r
load_rna_fusions(settings = NULL, ref_data = NULL)
```

## Arguments

- settings:

  PCGR run/configuration settings

- ref_data:

  PCGR reference data object
