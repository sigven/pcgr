# Function that assigns HTML links to dbNSFP prediction entries

Function that assigns HTML links to dbNSFP prediction entries

## Usage

``` r
append_dbnsfp_var_link(var_df)
```

## Arguments

- var_df:

  data frame with variant entries

## Value

var_df
