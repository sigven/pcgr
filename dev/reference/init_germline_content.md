# Function that initiates report element with germline variant information (CPSR)

Function that initiates report element with germline variant information
(CPSR)

## Usage

``` r
init_germline_content()
```

## Value

rep Report structure initialized for germline data (CPSR)
