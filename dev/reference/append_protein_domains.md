# Function that adds protein domain annotations (PFAM) for a list of variants and associated targeted

Function that adds protein domain annotations (PFAM) for a list of
variants and associated targeted

## Usage

``` r
append_protein_domains(var_df, ref_data = NULL)
```

## Arguments

- var_df:

  data frame with variants

- ref_data:

  PCGR/CPSR reference data object

## Value

var_df
