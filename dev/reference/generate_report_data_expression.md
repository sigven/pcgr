# Function that generates expression data for PCGR report

Function that generates expression data for PCGR report

## Usage

``` r
generate_report_data_expression(ref_data = NULL, settings = NULL)
```

## Arguments

- ref_data:

  PCGR reference data object

- settings:

  PCGR run/configuration settings
