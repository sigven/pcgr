# Function that generates a string with filtering criteria for callsets coming from tumor-only sequencing

Function that generates a string with filtering criteria for callsets
coming from tumor-only sequencing

## Usage

``` r
get_tumor_only_filtering_criteria(conf)
```

## Arguments

- conf:

  list object with PCGR run configuration settings

## Value

string with filtering criteria
