# Function that appends informative alteration names to sample variants based on gene symbol, consequence and HGVS annotations

Function that appends informative alteration names to sample variants
based on gene symbol, consequence and HGVS annotations

## Usage

``` r
append_alteration_name(var_data_df = NULL)
```

## Arguments

- var_data_df:

  Data frame of sample variants from VCF

## Value

var_data_df
