# Function that initiates report element with variant data

Function that initiates report element with variant data

## Usage

``` r
init_var_content()
```

## Value

rep Report structure initialized for variant data
