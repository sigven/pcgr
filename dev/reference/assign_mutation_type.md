# Function that assigns one of six mutation types to a list of mutations

Function that assigns one of six mutation types to a list of mutations

## Usage

``` r
assign_mutation_type(var_df)
```

## Arguments

- var_df:

  data frame with variants

## Value

var_df
