# Function that adds TFBS annotations (dbMTS) to genetic variant identifiers

Function that adds TFBS annotations (dbMTS) to genetic variant
identifiers

## Usage

``` r
append_tfbs_annotation(var_df)
```

## Arguments

- var_df:

  data frame with variants
