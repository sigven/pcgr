# Load expression similarity results

Load expression similarity results

## Usage

``` r
load_expression_similarity(settings = NULL)
```

## Arguments

- settings:

  PCGR run/configuration settings
