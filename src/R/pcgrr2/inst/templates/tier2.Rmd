### Tier 2 - other cancer mutation hotspots, curated mutations or predicted driver mutations

- A total of __`r nrow(report_data$variants_tier2_display)`__ unique, somatic variant(s) in the tumor sample are curated as disease-causing, predicted as driver mutations, or occur in previously detected mutation hotspots:
<br><br>

```{r tier2, echo=F}

myOptions <- list(paging = F,pageLength=10, searching=T,caching=F)
if(nrow(report_data$variants_tier2_display) > 10){
  myOptions <- list(paging = T,pageLength=10, searching=T,caching=F)
}
#colnames(report_data$variants_tier2_display) <- paste0("<span style=\"color:red\">",colnames(report_data$variants_tier2_display),"</span>")

mutation_hotspots <- dplyr::filter(report_data$variants_tier2_display, !is.na(CANCER_MUTATION_HOTSPOT))
non_hotspots <- dplyr::filter(report_data$variants_tier2_display, is.na(CANCER_MUTATION_HOTSPOT))

#if(nrow(mutation_hotspots) > 0){
  
#}

DT::datatable(report_data$variants_tier2_display, options = myOptions, escape=F, extensions = "Responsive")

```
<br><br>
