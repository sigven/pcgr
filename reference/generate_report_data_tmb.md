# Function that reads TSV file with TMB estimates from sample

Function that reads TSV file with TMB estimates from sample

## Usage

``` r
generate_report_data_tmb(settings = NULL)
```

## Arguments

- settings:

  PCGR run/configuration settings

## Value

tmb_estimate
