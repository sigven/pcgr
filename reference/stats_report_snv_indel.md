# Function that computes various variant statistics for SNVs/InDels from a callset object

Function that computes various variant statistics for SNVs/InDels from a
callset object

## Usage

``` r
stats_report_snv_indel(callset = NULL)
```

## Arguments

- callset:

  list object with callset data (SNVs/InDels)
