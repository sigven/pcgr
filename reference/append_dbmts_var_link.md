# Function that adds miRNA target annotations (dbMTS) to genetic variant identifiers

Function that adds miRNA target annotations (dbMTS) to genetic variant
identifiers

## Usage

``` r
append_dbmts_var_link(var_df)
```

## Arguments

- var_df:

  data frame with variants
