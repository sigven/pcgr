# Function that generates all contents of the cancer genome report (PCGR)

Function that generates all contents of the cancer genome report (PCGR)

## Usage

``` r
generate_report(yaml_fname = NULL)
```

## Arguments

- yaml_fname:

  Name of PCGR settings file produced by pre-reporting Python workflow
  (yaml)
